[build]
builder = "RAILPACK"
buildCommand = "npm install --omit=dev && npm --prefix ../frontend install && npm --prefix ../frontend run build"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
